# Process LINX Breakend File

Processes the `linx.breakend.tsv` file.

## Usage

``` r
linx_breakend_process(x)
```

## Arguments

- x:

  Path to `linx.breakend.tsv` file.

## Value

The input file as a tibble with column description.
