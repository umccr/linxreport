# Process LINX SVS File

Processes the `linx.svs.tsv` file.

## Usage

``` r
linx_svs_process(x)
```

## Arguments

- x:

  Path to `linx.svs.tsv` file.

## Value

The input file as a processed tibble.
